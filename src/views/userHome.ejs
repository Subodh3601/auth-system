<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign-Up</title>
    <link rel="stylesheet" href="style.css">
</head>

<body>
    <div id="outer-div">
        <header id="page-header">
            <h1><u>Authentication System</u></h1>
            <div id="header-div">
                <button class="page-header-button" type="button"><a href="/update-password">Update Password</a></button>

                <form method="get" action="/logout">
                    <button class="page-header-button" type="submit">Logout</button>
                </form>
            </div>
        </header>
        <div id="form-div">
            <h1 style="margin-top: 2rem;">User Page</h1>
            <% if(msg=="User SignIn Successful" || msg=="logged in through google" ){%>
                <P class="msg1">
                    <%=msg%>
                </P>
                <%}else{%>
                    <P class="msg">
                        <%=msg%>
                    </P>

                    <%}%>

        </div>
    </div>
</body>

</html>